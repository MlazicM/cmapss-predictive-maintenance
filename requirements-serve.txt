# Serving dependencies only -- what the Docker image installs.
#
# api/ imports src/, which needs numpy/pandas/scikit-learn (the scaler is
# unpickled, so scikit-learn must be present and version-compatible with the one
# that fitted it), TensorFlow to run the network, and joblib to load the scaler.
# The notebook stack in requirements.txt (jupyter, matplotlib, seaborn) and
# xgboost are not reachable from a request and are deliberately absent.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.1
tensorflow>=2.16
joblib>=1.3

fastapi>=0.110
pydantic>=2.0
uvicorn[standard]>=0.27
